---
import { URLWEB } from "@/consts";
---
<script src={`${URLWEB}assets/js/libs/bootstrap.bundle.min.js`} defer></script>
<script src={`${URLWEB}assets/js/libs/font-awesome/6.7.2/all.min.js`} defer></script>
<!-- <script>
    function ajustarMarginNavbar() {
    const navbar = document.querySelector('.navbar');
    const mainContent = document.querySelector('.main-content');
    const containerInner = document.querySelector('.container-inner');
    const hero = document.querySelector('.hero');

    if (!navbar || !mainContent) return;

    const alturaNavbar = navbar.offsetHeight;
    mainContent.style.marginTop = `-${alturaNavbar}px`;
    
    if (hero) {
        hero.style.paddingTop = `${alturaNavbar + 64}px`;
    }
    
    if (containerInner) {
        containerInner.style.paddingTop = `${alturaNavbar + 32}px`;
        containerInner.style.paddingBottom = `${(alturaNavbar / 2)}px`;
    }

    }

    window.addEventListener('DOMContentLoaded', ajustarMarginNavbar);
    window.addEventListener('resize', ajustarMarginNavbar);
</script>
    -->